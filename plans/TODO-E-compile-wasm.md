# Phase E: Run Codegen + Compile WASM

- [ ] Run `make codegen` and verify generated C++ files in bindings/generated/cpp/
- [ ] Run `make codegen` and verify generated TS files in bindings/generated/ts/
- [ ] Verify embind C++ compiles without errors
- [ ] Run `make build-bindings` and verify occt.wasm + occt.js produced
- [ ] Verify WASM file size is reasonable
